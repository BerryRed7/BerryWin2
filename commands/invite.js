const Discord = require ('discord.js')
const config = require ('mega_database')

exports.run = async (client, msg) => {
  msg.channel.send (`este comando não existe...para obter o invite do bot use (prefixo)help e obtenha o link de invite do bot`)
}